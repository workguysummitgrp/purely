spring:
    application:
        name: product-service
    data:
        mongodb:
            uri: mongodb://127.0.0.1:27017/purely_product_service
    jpa:
        hibernate:
            ddl-auto: update

server:
    port: 9010

eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8761/eureka/
    instance:
        hostname: localhost
